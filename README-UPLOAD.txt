Sprint C2 — inbound links for new comparison pages

EXTRACT AT THE REPOSITORY ROOT, not inside src/.
The archive already contains the src/ prefix, so dropping it at the repo
root lands files at src/data/... — dropping it inside src/ creates src/src/.

Files:
  src/pages/compare/[slug].astro   sibling-comparison block
  src/data/articles.ts             4 related links on 2 iron pages
  src/data/lastmod-manifest.json   regenerated
